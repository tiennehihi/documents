gnostics.Octal_literals_are_not_allowed_Use_the_syntax_0, start3, pos - start3, literal);
        return 9 /* NumericLiteral */;
      }
    } else {
      mainFragment = scanNumberFragment();
    }
    let decimalFragment;
    let scientificFragment;
    if (text.charCodeAt(pos) ===